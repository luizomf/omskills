# Claude Code Instructions

Canonical repository instructions live in [`AGENTS.md`](./AGENTS.md). Read and follow that file before changing this repository. This compatibility file exists for Claude-oriented tooling and does not override the canonical rules.

Skills are organized under:

- `skills/engineering/` for code, issue, and architecture work
- `skills/productivity/` for non-code workflow tools

Keep `.claude-plugin/plugin.json` synchronized with `.codex-plugin/plugin.json`. Catalog, README, frontmatter, and hard-coded reference requirements are defined in `AGENTS.md`.
