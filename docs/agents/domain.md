# Domain docs

This is a single-context repository.

Before changing skill behavior or workflow vocabulary, read:

- `CONTEXT.md` for canonical terms and relationships.
- Relevant records under `docs/adr/` for durable decisions.

Use glossary terms in skill descriptions, issue titles, tests, and documentation. If a proposed change conflicts with an ADR, surface the conflict explicitly instead of silently overriding it.
